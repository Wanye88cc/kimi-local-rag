---
description: Re-embed all tracked paths; pass "force" to wipe first (needed after model change)
---

Call the `rag_rebuild` tool with force=true if `$ARGUMENTS` contains "force", otherwise force=false. Pass the current project root as `dir`. Report the rebuild stats concisely.
