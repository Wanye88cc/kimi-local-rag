---
description: Wipe the local RAG index (tracked paths and config are preserved)
---

Confirm the user really wants to clear the index unless they already said so, then call the `rag_clear` tool passing the current project root as `dir`, and confirm the result in one sentence.
