---
description: Disable auto-injection (indexing and rag_query keep working)
---

Call the `rag_config` tool with key "ragEnabled" and value false, passing the current project root as `dir`. Confirm in one sentence.
