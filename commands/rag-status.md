---
description: Show local RAG index stats, storage location and gate configuration
---

Call the `rag_status` tool, passing the current project root as `dir`. Summarize concisely: store path and scope, files/chunks indexed, embedding model, last build time, whether auto-injection is enabled, tracked paths and exclude patterns.
