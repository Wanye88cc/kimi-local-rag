---
description: Enable auto-injection of relevant local context before each turn
---

Call the `rag_config` tool with key "ragEnabled" and value true, passing the current project root as `dir`. Confirm in one sentence.
