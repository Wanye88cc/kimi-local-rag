---
description: Incrementally re-index tracked paths (new/changed/deleted files)
---

Call the `rag_refresh` tool, passing the current project root as `dir`, and report how many files were indexed, unchanged and removed.
